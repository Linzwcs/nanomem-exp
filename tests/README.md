# Tests

Tests for schemas, dataset adapters, memory-system contracts, agent-system
contracts, evaluators, metrics, terminal reporters, and run reproducibility.

Protocol tests should verify that changing any result-affecting object changes the run config hash or protocol version.
